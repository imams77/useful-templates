# SonarQube Configuration
SONARQUBE_IMAGE=sonarqube:lts-community
SONARQUBE_PORT=9000

# PostgreSQL Configuration
POSTGRES_IMAGE=postgres:15-alpine
POSTGRES_USER=sonar
POSTGRES_PASSWORD=sonar
POSTGRES_DB=sonar

# JVM Options (optional - uncomment to customize)
# SONAR_WEB_JVM_OPTS=-Xmx512m -Xms128m
# SONAR_CE_JVM_OPTS=-Xmx512m -Xms128m
# SONAR_SEARCH_JVM_OPTS=-Xmx512m -Xms512m
